// Placeholder — the full Teams page (all sprint teams, sprint dropdown, pin
// "My Team") is owned by another teammate. This is just a mock so the
// dashboard's "See all" link has somewhere to land.
export default function TeamsPage() {
  return (
    <div className="flex min-h-[60vh] items-center justify-center">
      <p className="text-sm font-medium text-slate-400">
        Teams page coming soon.
      </p>
    </div>
  );
}
